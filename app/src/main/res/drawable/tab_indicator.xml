<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Gray underline (inactive state) -->
    <item
        android:left="-4dp"
        android:right="-4dp"
        android:bottom="0dp"
        android:top="45dp">
        <shape android:shape="rectangle">
            <solid android:color="@color/gray"/>
            <size android:height="1dp"/>
        </shape>
    </item>


</layer-list>