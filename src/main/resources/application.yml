server:
  port: ${PORT:8000}
  forward-headers-strategy: framework

spring:
  data:
    mongodb:
      uri: mongodb+srv://enowbrenda91106:clexis91106@cluster0.fnr4gwb.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
      database: clexis-db

defaultUser:
  username: admin
  email: admin.clexis@gmail.com
  password: admin
  role: ADMIN

api:
  ai:
    host: https://clexis-ai.up.railway.app
    port: 8000
    generateResource: ${api.ai.host}/api/v1/clexis_ai/tools/generate
    getStudentMatches: ${api.ai.host}:${api.ai.port.port}/api/v1/clexis_ai/tools/generate
    resourceRecommendation: ${api.ai.host}:${api.ai.port.port}/api/v1/clexis_ai/resources/propose

  notification:
    host: https://clexis-notification.up.railway.app
    port: 8080
    sendEmailCustom: ${api.notification.host}/api/v1/notification/sendEmail/custom
    sendEmailDefined: ${api.notification.host}/api/v1/notification/sendEmail/defined
